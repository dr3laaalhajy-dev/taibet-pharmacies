import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taiba.health',
  appName: 'خدمات طيبة الامام الصحية',
  webDir: 'dist'
};

export default config;
